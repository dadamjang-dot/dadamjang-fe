import { View } from 'react-native';
import { StyleSheet } from 'react-native-unistyles';

const SigninScreen = () => <View style={s.container} />;

const s = StyleSheet.create({ container: { flex: 1 } });

export default SigninScreen;
